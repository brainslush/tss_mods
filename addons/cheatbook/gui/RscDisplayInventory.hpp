class RscDisplayInventory : RscDisplayEmpty {
    class controls {
        class GroundContainer;
        class UniformContainer : GroundContainer {
            // onLBDblClick = QUOTE(call FUNC(openSheets));
        };
    };
};
